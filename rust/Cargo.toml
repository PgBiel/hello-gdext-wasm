[package]
name = "coolbeans"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]  # Compile this crate to a dynamic C library.

[features]
nothreads = ["godot/experimental-wasm-nothreads"]

[dependencies]
godot = { version = "0.3.1", features = ["api-4-3"]}
